---
name: delegate
description: 大きな探索・実装をOpus/Sonnetサブエージェントへ委譲してオーケストレーターのトークンを節約する。ユーザーが「サブエージェントで」「トークン節約して」と指示した時、またはタスクが大規模な時に使用。
---
# delegate — サブエージェント委譲の標準手順
1. タスクを「探索（読むだけ）」と「実装（書く）」に分解する。
2. 探索は model: sonnet、実装は model: opus のサブエージェント（Agentツール）に委譲する。
3. サブエージェントへの指示には必ず: 対象パス・完了条件・コミット規約・「push禁止/ローカルコミットのみ」を明記する。
4. 本体は結果の検証（git diff・テスト実行）と統合のみを行う。
5. 独立したタスクは並列で走らせる。
